"use client";

import DashboardLayout from "../dashboard/DashboardLayout";

export default function Hero3({ children }: { children: React.ReactNode }) {
  return (
    <section className="relative h-full w-full bg-gradient-to-r from-slate-700 via-slate-700 to-slate-800 
    overflow-hidden">
      
    </section>
  );
}
